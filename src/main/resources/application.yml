spring:
  datasource:
    url: jdbc:postgresql://localhost:6101/socialnetwork
    username: admin
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate.ddl-auto: validate
    show-sql: true
  liquibase:
    change-log: classpath:db/change-log/db.changelog-master.xml
    enabled: false